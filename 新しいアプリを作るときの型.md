# 新しいアプリを作るときの型

フリマ出品PRO AI（フリマプロ）で確立した、**ログイン・課金・回数制限つきWebアプリの作り方**です。
ここに書いてある順番どおりに進めれば、今回のように途中で何度も詰まることはありません。

最終更新: 2026-08-26

---

## このメモの使い方

新しいアプリを作るとき、Claudeに**この1行を伝えてください。**

> 開発したAIフォルダの「新しいアプリを作るときの型.md」を読んで、この型どおりに作って。

これだけで、下に書いてある構成・ファイル・手順・注意点がすべて共有されます。
「Googleログインを付けて」「決済を入れて」と個別に頼む必要はありません。

**すでにあるアプリに後から機能を足す場合も同じです。** 「型どおりに決済を入れて」と伝えてください。

---

## 1. 何を作る型なのか

| 要素 | 内容 |
|---|---|
| 見た目 | ブラウザで動く1ページのアプリ（React） |
| ログイン | Googleログイン ＋ メールアドレス＋パスワード |
| 課金 | Stripeの月額サブスク。購入・解約・返金まで自動 |
| 無料枠 | 1日◯回まで。日本時間の午前0時にリセット |
| 置き場所 | Vercel（サーバーもここ。別のサーバーは不要） |
| データ | Upstash Redis（アカウントと回数だけ。写真は保存しない） |

**この型の要点は「お金と権限の判断をすべてサーバーでやる」ことです。**
ブラウザ側の表示は飾りで、本当のプラン判定はサーバーの記録だけを見ます。
ここを曖昧にすると「払っていない人がProになれる」「払った人が使えない」が同時に起きます。

---

## 2. 技術の組み合わせ（2026-08時点で動作確認済み）

```
Vite 8 + React 19 + TypeScript 6 + Tailwind CSS 4
Vercel Functions（/api フォルダに置くだけでサーバーになる）
jose        … ログイン証明書（JWT）の発行と検証
redis       … Upstash Redis への接続
stripe      … 決済
@google/genai … Gemini（生成AI）
```

### Tailwind 4 の落とし穴（今回ハマった）

**`tailwind.config.js` に色を書いても効きません。** Tailwind 4 では CSS 側に書きます。

```css
/* src/index.css */
@import "tailwindcss";

@theme {
  --color-brand-500: #ff5722;
  --color-brand-600: #f03e0a;
  /* 50〜950 まで全部書く */
}
```

これを書かずに `.bg-brand-500 { background-color: #ff5722; }` のような手書きCSSで
凌いでいると、**`from-brand-600` や `to-brand-500` のようなグラデーション指定だけが
透明になり、背景の黒が透けて「黒くつぶれた」ように見えます。**
フリマプロではボタン・ロゴ・生成ボタンの3か所が黒く見えていました。

### Vercel Functions は「Webハンドラ形式」で書く

```ts
// api/health.ts
export function GET(request: Request): Response {
  return Response.json({ ok: true });
}
```

**`package.json` に `"type": "module"` がある場合、`/api` 配下の相対importには
必ず `.js` を付けてください。**

```ts
import { readSession } from '../server/http.js';   // ✅
import { readSession } from '../server/http';      // ❌ 本番で全APIが500になる
```

Vercelは `/api` のTypeScriptを「変換するだけ」で「1つにまとめない」ため、
拡張子が無いと本番で `ERR_MODULE_NOT_FOUND` になります。
**手元では動くのに本番だけ全部500** という、いちばん分かりにくい壊れ方をします。

---

## 3. ファイルの分担

```
src/                     画面（React）
  components/            画面の部品
  services/              サーバーへの問い合わせ
shared/
  listing.ts             AIへの指示文を組み立てる（画面とサーバーの両方から使う）
server/
  config.ts              環境変数とRedisのキー名を1か所に集める
  session.ts             ログイン証明書の発行と検証
  store.ts               Redisへの読み書き
  usage.ts               回数を数える唯一の場所
  password.ts            パスワードの暗号化と照合
  http.ts                リクエスト/レスポンスの共通処理
api/
  health.ts              設定が入っているかを外から確認する
  generate.ts            生成本体（ここで回数を判定して減らす）
  auth/google.ts         Googleログイン
  auth/register.ts       メール登録
  auth/login.ts          メールログイン
  auth/me.ts             今のログイン状態とプランを返す
  auth/logout.ts
  stripe/checkout.ts     決済画面へ送る
  stripe/portal.ts       解約画面を開く
  stripe/webhook.ts      支払いを権限に変える唯一の場所
public/
  tokushoho.html         特定商取引法に基づく表記
  terms.html             利用規約
  privacy.html           プライバシーポリシー
  version.txt            デプロイ確認用（後述）
```

### 特に大事な3つ

**`server/usage.ts` — 回数を数えるのはここだけにする。**
2か所で数えると必ず食い違います。ReRoomAIたちで実際に起きました。

**`api/stripe/webhook.ts` — 支払いが権限に変わるのはここだけ。**
ブラウザが「決済画面から戻ってきた」ことを根拠にProにしてはいけません。
戻ってきただけなら誰でも作れます。

**`server/config.ts` — 環境変数とRedisのキー名を1か所に。**
キー名が `xxx_ai:` と `xxx-ai:` に割れて、書く側と読む側が別を見ていた事故があります。

---

## 4. 手順（この順番で）

### Step 1 — Vercelプロジェクトを作る

1. GitHubにリポジトリを作り、コードをプッシュ
2. Vercelで「Add New → Project」→ そのリポジトリを選ぶ
3. Framework は **Vite**、Build Command は `npm run build`、Output は `dist`
4. 独自ドメインを付ける（Settings → Domains）

**Vercelは Pro プラン（月$20）が必須です。** Hobbyは非商用限定で、決済を受け取ると規約違反。
**すでにチームがProなら、同じチームに作れば追加費用はかかりません**（プロジェクト数は無制限）。

### Step 2 — 環境変数

`.env` をローカルに作り、`Vercelに環境変数を入れる.command` で一括投入します。

```
GOOGLE_CLIENT_ID=            Googleログイン用
VITE_GOOGLE_CLIENT_ID=       同じ値（画面側で使う。これだけはVITE_で正しい）
JWT_SECRET=                  64文字のランダム文字列
GEMINI_API_KEY=              Gemini
STRIPE_SECRET_KEY=           sk_live_...
STRIPE_WEBHOOK_SECRET=       whsec_...
KV_REDIS_URL=                Upstash Redis
PRO_PRICE_JPY=1480
FREE_DAILY_LIMIT=3
GUEST_DAILY_LIMIT=3
PRO_HQ_MONTHLY_LIMIT=200
PRO_FAIR_USE_DAILY_LIMIT=100
```

**`VITE_` が付いた変数はブラウザに丸見えになります。**
APIキー・秘密鍵には絶対に付けないこと。GoogleのクライアントIDだけは公開前提なのでOKです。

`JWT_SECRET` の作り方（ターミナルで実行）:

```
openssl rand -base64 48
```

**Vercelの環境変数は、入れただけでは反映されません。必ず再デプロイしてください。**

### Step 3 — Googleログイン

1. Google Cloud Console → 認証情報 → OAuth 2.0 クライアントID（ウェブアプリケーション）
2. **「承認済みのJavaScript生成元」** に本番URLを入れる（リダイレクトURIではありません）
   ```
   https://<アプリ名>.smart-ai-portal.com
   ```
3. 発行されたクライアントIDを `GOOGLE_CLIENT_ID` と `VITE_GOOGLE_CLIENT_ID` の両方へ

サーバー側では、送られてきた証明書を**必ず署名検証**します（`jose` の `createRemoteJWKSet`）。
中身を読むだけにすると、誰でも他人になりすませます。3アプリで実際にそうなっていました。

### Step 4 — Redis（Upstash）

Vercel の Storage タブから Upstash Redis を作ると、環境変数が自動で入ります。
`server/config.ts` では `REDIS_URL` / `KV_URL` / `KV_REDIS_URL` のどれでも拾えるようにしておきます。

### Step 5 — Stripe（テスト → 本番）

**まずサンドボックス（テストモード）で通し切ってから本番に移します。**

テストモードでやること:

1. Webhook を登録
   - URL: `https://<ドメイン>/api/stripe/webhook` ← **末尾のパスを忘れない**
   - イベント4つ:
     ```
     checkout.session.completed
     customer.subscription.updated
     customer.subscription.deleted
     charge.refunded
     ```
2. カスタマーポータルを有効化（設定 → 請求 → カスタマーポータル）
   - 「サブスクリプションのキャンセル」をオン
   - タイミングは「**請求期間の終了時**」
   - 請求履歴の表示をオン
3. テストカード `4242 4242 4242 4242` で購入 → Pro → 解約 → 返金 まで確認

本番モードに移すとき:

- **「データをコピーしない」を選ぶ**（価格はコードの中で組み立てているのでコピー不要）
- **Webhookもカスタマーポータルも、本番モードで作り直しが必要です。** コピーされません
- **カスタマーポータルは、設定が正しく見えていても「保存」を一度押してください。**
  本番モードで一度も保存していないと、解約画面がAPIから開けません
- `sk_live_...` と新しい `whsec_...` を `Stripeの鍵を設定する.command` で投入

### Step 6 — 法的な3ページ

`public/` に静的HTMLで置きます（Reactの中に作らない。検索エンジンとお客さんが直接開けるように）。

- `tokushoho.html` — 特定商取引法に基づく表記
- `terms.html` — 利用規約
- `privacy.html` — プライバシーポリシー

**必ずフッターからリンクしてください。**

**書いた内容と実装は必ず一致させること。** 「マイページから解約できます」と書いてあるのに
その画面が存在しない、という状態が3アプリで残っていました。書く前に作ります。

個人事業主の場合、住所と電話番号は「請求があれば遅滞なく開示します」で構いませんが、
**問い合わせ用のメールアドレスは必ず載せてください。**

### Step 7 — 公開前チェック

```
□ /api/health が全部 set になっている
□ ブラウザのファイルにAPIキーが混ざっていない
   → npm run build && grep -c "AIza\|sk_live\|whsec_" dist/assets/*.js  が 0
□ 実際のお金で 購入 → Pro表示 → 解約 → 返金 → Free復帰 を通した
□ Webhookが4種類とも 200 OK になっている
□ スマホの幅（360px・390px）で崩れていない
□ 特商法・利用規約・プライバシーの3ページがフッターから開ける
□ 料金表に書いた機能が、実際に全部動いている
```

**最後の項目は特に重要です。** 実装していない機能を料金表に書くと景品表示法（優良誤認）にあたります。
フリマプロでは「ジャンル別プロンプトカスタム」「優先生成」の2つが、
画面にはあるのに生成には一切影響していませんでした。

---

## 5. 必ず作る4つの `.command`

ターミナルが苦手でも、**ダブルクリックだけ**で作業が完結します。
フリマプロのフォルダから丸ごとコピーして、中のパスを変えれば使えます。

| ファイル | 何をするか |
|---|---|
| `Vercelに環境変数を入れる.command` | `.env` を読んでVercelへ一括投入 |
| `Stripeの鍵を設定する.command` | 鍵を**画面に出さずに**受け取り、`.env`とVercelへ入れて再デプロイ |
| `更新をGitHubに送る.command` | ロック削除 → add → commit → push |
| `本番にデプロイする.command` | `npx vercel --prod` |

**鍵を受け取る `.command` では `read -rs` を使って画面に表示しないこと。**
スクリーンショットに写り込むと作り直しになります（実際に1回やりました）。

`✓ Ready` が出たあとの `Enterキーで閉じます...` は**完了の合図**です。止まっているわけではありません。

---

## 6. デプロイできたかを一瞬で確かめる方法

**`public/version.txt` に日付入りの文字列を置いてください。**

```
ui-2026-08-26f
```

デプロイ後にこれを開けば、新しいものが載ったかが確実に分かります。

```
https://<ドメイン>/version.txt
```

**「画面が変わらない」の原因はほぼ全部ブラウザのキャッシュです。**
フリマプロでは、本番は正しく更新されているのに古い画面を見続けて1時間ほど溶かしました。

- Mac: `Command + Shift + R`（普通の再読み込みでは足りません）
- それでもダメなら、URLの末尾に `?v=2` を付ける
- いちばん確実なのは、**一度も開いたことのない別のブラウザで開くこと**

---

## 7. 料金と原価の考え方

**Proを「無制限」と書くなら、原価の上限を先に決めてください。**

フリマプロの計算（月1,480円・Stripe手数料3.6%を引いて手取り約1,427円）:

| モデル | 入力 | 出力 | 1回あたり目安 |
|---|---|---|---|
| Gemini 2.5 Pro | $1.25 / 1M | $10 / 1M | 約4円 |
| Gemini 2.5 Flash | $0.30 / 1M | $2.50 / 1M | 約0.9円 |

- 「高精度モデルを1日20回」にすると 20×30×4円 = **月2,400円**。**赤字です**
- そこで「**高精度は月200回まで。それ以降は通常モデルで無制限**」にしました
  → 最悪でも月800〜1,200円。手取り1,427円の範囲に収まります
- あわせて不正利用対策として「1日100回」の上限も入れています（画面には出しません）

**「無制限」と書きながら内部に上限を持つ場合、利用規約には必ず書いてください。**
書いていないと約束違反になります。

---

## 8. 今回踏んだ地雷の一覧（同じ穴に落ちないために）

| 症状 | 原因 | 対策 |
|---|---|---|
| 本番でAPIが全部500 | `/api` の相対importに `.js` が無い | 全部に `.js` を付ける |
| デプロイが「Blocked」 | コミットのメールアドレスがGitHubと不一致 | `git config user.email <GitHubの noreply アドレス>` |
| グラデーションが黒くつぶれる | Tailwind 4 に色を登録していない | `@theme` に `--color-*` を書く |
| 環境変数を入れたのに反映されない | 再デプロイしていない | 入れたら必ずデプロイ |
| Webhookが届かない | URLの末尾のパス漏れ／本番で作り直していない | `/api/stripe/webhook` まで入れる |
| 解約画面が開かない | 本番モードでポータルを保存していない | 内容を変えなくても「保存」を押す |
| Googleログインが動かない | リダイレクトURIに入れていた | **JavaScript生成元**に入れる |
| アカウントを変えると無料枠が増える | アカウント単位でしか数えていない | アカウントと回線（IP）の**多いほう**で数える |
| 画面が変わらない | ブラウザのキャッシュ | `version.txt` で本番を確認 → 別ブラウザで開く |
| スマホで文字が縦一列になる | 横並びが収まりきらない | 狭い画面では上下2段に組み替える |
| Claudeが `git push` できない | 認証情報が無い | `.command` をダブルクリック |
| `.git/index.lock` が残る | Claudeはファイルを削除できない | `_to_delete/` へ退避、または自分で `rm -f` |

---

## 9. 無料枠のずるを防ぐ数え方

```
無料の人の使用回数 = max（そのアカウントの回数, その回線の回数）
```

アカウントだけで数えると、Googleアカウントを切り替えるだけで無料枠が復活します。
回線だけで数えると、家族や同じ職場の人が巻き添えになります。
**両方数えて多いほうを採用する**のが、いちばん被害が少ない形です。

- リセットは**日本時間の午前0時**（UTCのままだと朝9時にリセットされて不自然）
- Proの人はこの判定を通しません（無制限なので）
- **回数を減らすのは生成が成功したあと。** 先に減らすと、失敗しただけで枠が消えます

---

## 10. 写真を扱うときの決まり

- **サーバーに保存しない。** 生成に使ったらそのまま捨てる
- プライバシーポリシーに「保存しません」と明記する
- 生成結果の履歴はブラウザの中だけ（最大10件）に置く

保存しなければ、流出させようがありません。運用も監査も、そのぶん楽になります。

---

## 11. 完成後にやること

- **Gemini APIの請求を月1回見る。** 上限値（`PRO_HQ_MONTHLY_LIMIT`）を実績に合わせて調整
- **Stripeの「支払い」画面が売上そのもの**
- コードを直したら `更新をGitHubに送る.command` → `本番にデプロイする.command` の順

---

## 12. 参考にする既存アプリ

| 目的 | 見るアプリ |
|---|---|
| Vite + React + Vercel Functions の完成形 | **フリマ出品PRO AI（フリマプロ）** |
| Next.js での同じ仕組み | ReRoomAI / WallAI / Studio AI |
| ブログの自動生成 | ReRoomAI（`scripts/generate-blog-post.mjs`） |

**新規に作るなら、フリマプロをそのまま複製して名前と中身を差し替えるのがいちばん早いです。**
ログイン・課金・回数制限・法的ページが全部そろっていて、本番で実証済みの唯一の構成です。
