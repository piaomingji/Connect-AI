# 引き継ぎメモ

新しい会話を始めたら、最初に「開発したAIフォルダの引き継ぎメモを読んで」と伝えてください。
これを読めば、今どこまで進んでいるかが分かります。

最終更新: 2026-08-26

## 次にやること（ここだけ読めば再開できます）

**フリマ出品PRO AI（フリマプロ）は 2026-08-26 に公開まで完了しました。**
本番の決済・解約・返金まで実地で確認済みです。詳細はこのメモの末尾を見てください。

**新しいアプリを作るときは、まず `開発したAI/新しいアプリを作るときの型.md` を読んでください。**
Claudeには「開発したAIフォルダの『新しいアプリを作るときの型.md』を読んで、この型どおりに作って」
と伝えるだけで共有できます。


**ReRoomAI・WallAI・Studio AI は完了しています。** 決済（購入・解約・返金）も、利用者が自分で
解約できる導線も、Vercelの商用対応も済んでいます。手を入れる必要はありません。

**ブログの画像の件は2026-08-21に完了しました（プッシュ済み）。**

**8/22分のブログ記事は3アプリとも入りました（Run workflow 成功・確認済み）。**

残っているのは、検査と再試行を入れた2本目のコミットをプッシュすることだけです。
GitHub側が先に記事をコミットしているので、取り込んでからプッシュします。

```
cd "/Users/yuri/Downloads/AI멘토 Jay/開発したAI/ReRoomAI"
git pull --rebase --autostash
git push
cd "/Users/yuri/Downloads/AI멘토 Jay/開発したAI/WallAI"
git pull --rebase --autostash
git push
cd "/Users/yuri/Downloads/AI멘토 Jay/開発したAI/Studio AI"
git pull --rebase --autostash
git push
```

触っているファイルが両側で完全に別なので、衝突は起きません。
プッシュすると「ブログ生成スクリプトの検査」が自動で走るので、緑になれば完了です。

そのあとに残るのは Talkie（genbatalk）の公開前準備だけです。

### Talkie — 2026-08-26 に18コミット（すべてプッシュ済み・最新 `1498b11`）

翻訳の否定の反転、前の発話の混入、中国語とアラビア語の読み上げが鳴らない不具合、Android の
音量のばらつき、**エラーが一切画面に出ていなかった件**、有料会員の回数制限。すべて直して
実機テスト（iPhone・Android とも6項目）も通っています。詳細はこのメモの
「**Talkie の翻訳精度を直した**」以降を読んでください。

**明日いちばんに見るのはこの1つです。**

> **音の近い語の取り違えが直っていません。** 中国語で 收据（shōujù／レシート）と言うと
> 手机（shǒujī／携帯）や 手月（中国語に無い語）と聞き取られ、携帯・手帳と訳されます。
> 何度言い直しても同じです。
>
> **最後に打った手が未検証です。** 校正パスの思考の予算を 0 → 2048 に戻しました（`1498b11`）。
> JOB 2 は推論の仕事なのに、別件の対処で思考を切っていたのが原因という読みです。
> **これで直るかどうかを、まず確かめてください。**
>
> ログの `reading unclear: ...` が出れば当たり、`reading looks settled` のままなら外れです。
> 外れたときの次の一手は「未解決の課題」の節に書いてあります。

**確認の手順:** アドレスの末尾に `/?debug=1` を付けて開く（一度開けば端末が記憶します）。
靴のサイズ交換のような場面で「レシートはお持ちですか」を中国語で言い、琥珀色の帯が出るか見る。

---

## 5つのアプリ

| アプリ | フォルダ | 公開アドレス | Vercelプロジェクト | Stripeアカウント |
|---|---|---|---|---|
| ReRoomAI（ミセルリフォーム） | `ReRoomAI` | reroom.smart-ai-portal.com | reroom-ai | ミセルリフォーム |
| WallAI | `WallAI` | wall.smart-ai-portal.com | wall-ai | WallAI |
| Studio AI | `Studio AI` | studio.smart-ai-portal.com | studio-ai | Studio AI |
| Talkie（旧 GenbaTalk） | `genbatalk` | talkie.smart-ai-portal.com | genbatalk | Talkie |
| フリマ出品PRO AI（フリマプロ） | `フリマ出品PRO AI（フリマプロ）` | frima.smart-ai-portal.com | frima-pro-ai | フリマプロ |

**Stripeアカウントはアプリごとに別々です。** ウェブフックを登録するときは、
左上のアカウント名とURLが一致していることを必ず確認してください。ここを間違えて半日ぶん無駄にしました。

---

## 完了していること（2026-08-19〜20）

### ReRoomAI / WallAI / Studio AI の3つ

**無料枠が効いていなかった問題** — すべて修正・本番で確認済み

- 利用者情報がサーバーのメモリに置かれ、毎回消えていた（ログインのたびに無料枠が再付与されていた真因）
- Googleの認証情報を署名検証せず中身を読むだけだった
- セッションを署名なしでも受け入れていた（なりすまし可能だった）
- 回数カウンタが `@vercel/kv`（HTTP接続）経由で、認証情報が未設定のため読み取りは常に0・書き込みは破棄
- キー名が `xxx_ai:` と `xxx-ai:` の2種類に分かれ、書く側と読む側が別を見ていた
- 上限判定が2か所にあり数字が食い違っていた
- 回数を二重に数えていた
- 生成に失敗してもクレジットだけ減っていた（生成成功後に引くよう変更）
- 署名鍵がソースに直書きされていた
- 未ログインなのに画面はログイン中に見えていた
- WallAI固有: 回数の記録が72時間で消え、3日ごとに無料枠が復活していた
- ReRoomAI固有: リロードのたびに無料枠が50に戻る処理が残っていた
- ReRoomAI固有: 独自ドメインが別プロジェクトを指しており、修正が届いていなかった

**決済の問題** — すべて修正・本番で確認済み

- 支払いがサーバーで一度も確認されていなかった（払った人が使えず、払わない人が画面上だけProになれた）
- ウェブフック（`app/api/stripe/webhook/route.ts`）を新規作成し、署名検証したうえでクレジット付与
- 購入時のリクエストに `credentials: 'omit'` が指定され、ログイン情報が送られず**誰も購入できなかった**
- Stripeの鍵が未設定のとき無料でプランを配る仕組みが本番に残っていた（撤去）
- ウェブフックが全部間違ったStripeアカウントに登録されていた
- WallAIの鍵が別アカウントのものだった（決済画面が「サンドボックス」表示になっていた）
- 購入者が端末単位の上限で止められていた（`hasPurchased` の印を追加して解決）
- Studio AI: 追加パック購入者が画像を保存できなかった（`canSave` で判定するよう変更）

**確認方法** — 3アプリとも、実際に1,480円を支払い、クレジット付与・生成・保存まで通ることを確認済み。
Stripe側でウェブフックが `200 OK` を返すことも確認。テスト購入は返金済み。

### 月額Proプラン（2026-08-20 / ReRoomAIで検証）

**購入 → 解約まで実地で通しました。** 宣伝前に一番不安だった箇所です。

- 購入すると webhook の `checkout.session.completed` で `plan=pro`・クレジット+100・`hasPurchased=true` が入る
- Proは無制限扱い。`deductUserCredit` が pro のときは回数を減らさずに成功を返す
- Stripeで**即時**解約すると `customer.subscription.deleted` が届き、`plan=free` に戻る。
  買ったクレジットは残る（意図どおりの設計）
- 4,980円を実際に支払って確認し、そのあと返金済み。ウェブフックは失敗0件

### Pro表示の修正（2026-08-20 / 3アプリともプッシュ済み）

Pro会員なのに「残り999回」と表示される問題を修正しました。
- `lib/auth.ts`: pro のとき `remainingCredits: 999` というダミー値を返していたのをやめ、実際の残高を返す
- `components/Studio.tsx`: ログイン判定より先に `user.plan` を見て「使い放題」と表示（ReRoomAI・WallAI）
- Studio AI は画面上に該当表示がないため `lib/auth.ts` のみ

**WallAI と Studio AI は購入までは流していませんが、設定は確認済みです。** 実装は3アプリとも同一で
（`subscription_data.metadata.userId` も3つとも正しく入っている）、月額で新しく通る道は
`customer.subscription.deleted` だけ。その購読が2アプリとも入っていること、宛先URLとアカウントが
一致していることを2026-08-20に確認しました（失敗0件）。追加パックの検証で各アプリのキー・署名・
アカウントは既に実証済みなので、月額の再購入は不要と判断しています。

---

### 利用者が自分で解約できるようにした（2026-08-20 / 3アプリ）

**特商法ページには「マイページ/設定より解約手続きを行うことができ」と書いてあるのに、
その導線がどこにも無い**という食い違いがありました。今日までの解約テストが通っていたのは、
Stripe管理画面から操作していたからで、利用者側の経路は一度も存在していません。
月額を宣伝する前に塞ぐべき穴だったので、3アプリとも実装しました。

- `app/api/billing-portal/route.ts` を新規作成。Stripeのカスタマーポータルを開く
- ヘッダーのメニューに「お支払い・解約」を追加（購入したことがある人にだけ表示）
- 顧客IDはこれまでどこにも保存していなかったので、ウェブフックで控えるよう追加。
  それ以前に購入した人には、メールアドレスから顧客を引き当てるフォールバックを入れてある

あわせて、ウェブフックに2つの処理を追加しました。

- `charge.refunded`: 返金されたらプランを無料に戻し、付与したクレジットも差し引く。
  定期課金の返金なら契約自体も止める（放置すると翌月また請求されるため）。
  金額から売った内容を逆算している（4,980=Pro / 19,800=法人 / 1,480=追加パック）ので、
  **価格を変えるときは `planFromAmount` も直すこと。**
- `customer.subscription.updated`: 3アプリとも購読しているのに無視していた。
  「請求期間の終わりに解約」も、カード期限切れで止まった場合も、これで無料に戻る。

**Stripe側の設定も3アカウントとも完了しています**（カスタマーポータルの有効化・キャンセル許可・
請求期間の終了時にキャンセル・請求履歴の表示、およびウェブフックへの `charge.refunded` 追加）。

**検証済み（2026-08-20 / ReRoomAIで実地）** — 今度は利用者と同じ経路を通しました。

1. メニューの「お支払い・解約」からStripeのポータルが開く
   （顧客IDが未保存のアカウントでも、メールからの引き当てで開けることを確認）
2. Proを購入 → ポータルの「サブスクをキャンセル」で解約
   → Stripeのイベントに「ソース: カスタマーポータル」として記録される
3. 解約直後もアプリはProのまま。**これが正しい挙動**（期間末解約なので支払った月は使える）
4. 全額返金 → 契約が即停止し、プランが無料に戻り、クレジットは119のまま
5. ウェブフックは合計6件・失敗0件

WallAI と Studio AI は購入検証をしていませんが、実装は3アプリ同一で、ウェブフックの購読
イベントとポータル設定が同じであることを確認済みです。**追加の購入検証は不要と判断しました。**

なお、ウェブフック画面の「イベントの配信 合計◯件」は集計が数分遅れます。
届いていないように見えても、少し待つか「イベントの配信」タブで1件ずつ見てください。

**WallAI と Studio AI でも「お支払い・解約」を開いて確認済み（購入はしていません）。**

- WallAI: ポータルが開いた（設定・API・デプロイとも正常）
- Studio AI: 顧客が見つからず案内メッセージが出た。これも**APIが正常に動いた結果**

履歴が空／顧客が無いのは、**1,480円の追加パックがStripeの顧客を作らない買い方**だったためです
（一回払いは既定では顧客が作られない）。月額サブスクは必ず顧客が作られるので、Proを契約した人は
確実にポータルから解約できます。とはいえパックだけ買った人に「記録が見つかりません」と出るのは
不親切なので、次を追加しました。

- 一回払いの決済にも `customer_creation: 'always'` を指定。今後パックを買った人も領収書を見られる
- 契約が無い場合の文言を「ご契約中の月額プランはありません…」に変更し、赤字をやめた
- **この修正より前にパックだけ買った人には顧客が存在しません。** その人たちは上の案内文でカバー

### Vercel を Pro に切り替え（2026-08-20）

Hobbyは規約上、非商用・個人利用に限られます。決済を受け取る時点で商用にあたり、違反すると
アカウントやデプロイが**停止**されます。すでに宣伝を開始していたため、この日に切り替えました。

- 料金は**開発者1人あたり月$20**。プロジェクト数は無制限なので、**4アプリ合わせて$20**です
- 既存のチームをその場でアップグレードしたので、環境変数・ドメイン・デプロイはそのまま
- **新しいチームを作ってプロジェクトを移す方法は取らないこと。** 環境変数が引き継がれず、
  ドメインの再設定も必要になります

---

## 決済まわりで残っている穴

- **一部返金（金額を指定した返金）は自動では戻しません。** 何を返したのか判断できないためです。
  全額返金のときだけ自動で戻ります。
- 月次更新（2回目以降の請求）そのものは未処理です。Proは無制限判定なので今は実害ありませんが、
  将来クレジット制に変えるなら `invoice.payment_succeeded` の処理が要ります。

---

## ブログのアイキャッチ画像が壊れていた問題（2026-08-20 / 3アプリ）

**原因は、Googleが画像生成APIを廃止したことでした。** スクリプトが使っていた
`imagen-3.0-generate-002` は2025年11月に提供終了し、後継の `imagen-4.0` 系も
**2026-08-17に提供終了**しています。この日を境に3アプリとも画像生成が毎日失敗し、
コードに書かれていた代替画像に落ちていました。

- ReRoomAI → Pollinations AI（1024x576・50KB程度の低品質画像）
- Studio AI / WallAI → Unsplashの固定URL。しかもURLの一部は既にリンク切れで、
  ブログ上に画像が表示されない状態だった。記事の内容とも無関係な画像だった

**対応（3アプリとも `scripts/generate-blog-post.mjs` を修正）**

- 画像モデルを **`gemini-3-pro-image`（Nano Banana Pro）** に変更。
  失敗したら `gemini-3.1-flash-image` に切り替え、各モデル2回まで試す
- 新旧2つのAPI形式（`generateContent` と `interactions`）の両方に対応させたので、
  SDKのバージョンが上がっても動くようにしてある
- **Pollinations と Unsplash のフォールバックは全部消しました。**
  画像が作れなかった場合は記事を追加せずワークフローを失敗させます。
  おかしな画像が公開されるより、その日の記事が1本増えないほうがマシという判断です
- 画像プロンプトを記事テーマに合わせるよう強化（WallAIで室内写真が出る、
  Studio AIで背景の記事なのに屋外の写真が出る、といった食い違いの対策）

**画像1枚あたり約$0.134（2K）です。** 3アプリ×1日1本で月$12前後。
価格を抑えたい場合は `IMAGE_MODELS` の先頭を `gemini-3.1-flash-image` にすれば半額です。

**画像は必ず圧縮してから保存します。** 生成直後は 2752x1536・約3MB あり、そのままだと
ブログの読み込みが重くなるためです。幅1600px・品質82のJPEGにして **約170KB** に落としています
（見た目はほとんど変わりません）。圧縮には `sharp` を使うので、3アプリの `package.json` の
devDependencies に追加しました。**`npm install` を忘れると `npm ci` が失敗します。**
sharp が無い環境では圧縮せずそのまま保存します（生成自体は止まりません）。

**過去記事の作り直し用に `scripts/regenerate-eyecatch.mjs` を3アプリに追加しました。**

```
node scripts/regenerate-eyecatch.mjs --dry-run        # 対象を一覧表示するだけ（無料）
node scripts/regenerate-eyecatch.mjs --compress-only  # 既存画像の圧縮だけ（無料）
node scripts/regenerate-eyecatch.mjs --limit 1        # 1枚だけ作って様子を見る
node scripts/regenerate-eyecatch.mjs                  # 対象をすべて作り直す
```

幅1376px未満の画像・外部URL・ファイル欠損を「代替画像」と判定して作り直します。
失敗した記事は据え置かれるので、同じコマンドを再実行すれば失敗ぶんだけ再挑戦します。

**3アプリまとめて実行するには、`開発したAI/ブログ画像を作り直す.command` を使ってください。**
sharpの導入から圧縮・作り直しまで順番に流します。

### 作り直しの結果（2026-08-20 完了・プッシュ済み）

| アプリ | 記事数 | 画像の合計 | 平均 | 欠損 | 外部URL |
|---|---|---|---|---|---|
| ReRoomAI | 27 | 4.2MB | 160KB | 0 | 0 |
| WallAI | 25 | 4.9MB | 201KB | 0 | 0 |
| Studio AI | 22 | 1.7MB | 80KB | 0 | 0 |

**Studio AI には、記事ごとに人物（年代・性別・髪型）を変える `PERSONAS` の仕組みを入れました。**
以前は似た雰囲気の女性のストック写真ばかり並んでいたための対策です。10通りの人物像から記事ごとに
自動で割り当て、就活・子供・婚活など記事の内容が優先されます（子供の証明写真の記事には赤ちゃんが出ます）。

---

## 自動生成が503で止まった件（2026-08-21 / Studio AI）

**8/21の朝、Studio AI だけ記事が生成されませんでした。** GitHub Actions のログを見ると、
画像ではなく**記事本文を書くステップ**で Gemini が 503 を返していました。

```
"code":503, "message":"This model is currently experiencing high demand..."
  at async generateArticle
```

Googleの一時的なサーバー混雑です。**この部分には再試行が無く、1回失敗しただけで丸ごと諦める作り**
でした。ReRoomAI と WallAI はたまたま混雑に当たらなかっただけで、いつどれが落ちてもおかしくない
状態でした。

**対応（3アプリともプッシュ済み）**

- テーマ生成・記事本文生成・画像プロンプト生成のすべてに `withRetry` を追加。
  503 / 429 / 500 / タイムアウトなどを検知し、15秒 → 30秒 → 45秒と待って最大4回試す
- 画像生成のモデル切替時の待ち時間を4秒 → 20秒に延長
- **画像生成そのものは `withRetry` で包んでいません。** `renderImageWithFallback` が既に
  再試行しているので、二重にすると1回の実行で最大16枚生成して費用が跳ね上がります
- 3アプリが同時にAPIを叩かないよう実行時刻をずらしました

| アプリ | 実行時刻（日本時間） | cron (UTC) |
|---|---|---|
| ReRoomAI | 午前3時00分 | `0 18 * * *` |
| WallAI | 午前3時20分 | `20 18 * * *` |
| Studio AI | 午前3時40分 | `40 18 * * *` |

**GitHub Actions が失敗したときの調べ方**（今回これで原因にたどり着きました）

1. `https://github.com/piaomingji/<リポジトリ>/actions` を開く
2. 赤い×のついた実行をクリック → 左の「generate」をクリック
3. 「Generate new blog post」を展開すると、エラーの全文とスタックトレースが読めます

失敗したときはメール通知も届きます。記事が1本増えていない日は、まずここを見てください。

---

## 自動生成が withRetry is not defined で止まっていた件（2026-08-22 / 3アプリ）

**8/22の朝、ReRoomAI・WallAI・Studio AI の3つとも記事が追加されていませんでした。**
GitHub Actions はいずれも「Generate new blog post」で26〜31秒で失敗しています。

```
ReferenceError: withRetry is not defined
  at generateImage (scripts/generate-blog-post.mjs)
```

**原因は、前日に入れた再試行の仕組みが自分のコミットで巻き戻ったことです。**

- 8/21 06:25 のコミットで `withRetry` を追加した
- 8/21 06:42 の「アイキャッチ画像に変化をつける」コミットが**古い版を土台に作られていた**ため、
  `withRetry` の定義と、テーマ生成・記事本文生成の呼び出しを丸ごと消してしまった
- ところが画像プロンプト生成の行だけ `withRetry(...)` を呼ぶ形で残った
- 結果、記事本文まで書けたあと画像に入る直前で必ず落ちる。**3アプリとも同じ形で壊れていた**
- 画像の待ち時間も 20秒 → 4秒 に巻き戻っていた
- 同じ壊れ方が `scripts/regenerate-eyecatch.mjs` にもあった（作り直しスクリプトも動かない状態だった）

これはメモに書いてある「`git pull --rebase --autostash` で修正が巻き戻ることがある」の実例です。
**再試行のような「定義」と「呼び出し」が離れている変更は、プッシュ後に
`grep -n withRetry scripts/*.mjs` で定義が残っているか確認してください。**

**対応その1: 壊れていた箇所を直した（3アプリともコミット済み・プッシュ済み）**

- `withRetry` / `isRetryableApiError` / `API_RETRIES` の定義を `generate-blog-post.mjs` と
  `regenerate-eyecatch.mjs` の両方に復元
- テーマ生成・記事本文生成の呼び出しを再試行付きに戻した
- 画像モデル切替の待ち時間を 4秒 → 20秒 に戻した

**対応その2: 同じことが二度と起きないようにした（コミット済み・プッシュ待ち）**

直すだけでは、また巻き戻ったときに翌朝まで気づけません。仕組みで止めます。

- **`scripts/smoke-test-blog.mjs` を追加しました。** APIもお金も一切使いません。
  - 呼んでいるのに定義が無い関数を静的に探す（**実行されない分岐も見つかる**）
  - `@google/genai` を偽物に差し替え、一時フォルダで両スクリプトを最後まで実際に走らせる
  - 503を1回返す偽物でも試し、再試行して続行することを確認する

    手元で確かめたいときは、アプリのフォルダで `node scripts/smoke-test-blog.mjs` です。
    今回壊れていた8/22版に掛けると、4件すべて検知して赤くなることを確認しました。

- **`.github/workflows/blog-script-check.yml` を追加しました。**
  `scripts/` を触った push で必ず走ります。**巻き戻しはここで止まります。**
  メールも届くので、翌朝の記事を待つ必要はありません。
- **毎日のワークフローも変えました。** 生成の前に同じ検査を走らせ、
  生成そのものは **10分あけて最大3回** 試します。
  Geminiの一時的な混雑くらいなら、これで勝手に立ち直ります。

**これで残るリスクは「3回とも失敗するほどのGoogle側の障害」「APIキーの失効」
「無料枠・請求の上限」くらいです。** そのときはGitHubから失敗メールが届きます。

**検証方法（お金もAPIも使わずに確認しました）** — `@google/genai` を偽物に差し替えて
3アプリのスクリプトを最後まで通し、記事の追加と画像の保存が成功することを確認済みです。
同じ手順で修正前のコードを流すと、上記の `ReferenceError` が再現します。
503を1回返す偽物でも試し、「15秒待ってから再試行します」と出て正常に続くことを確認しました。

**やってほしいこと** — ターミナルで1行ずつ実行してください。

```
cd "/Users/yuri/Downloads/AI멘토 Jay/開発したAI/ReRoomAI"
git push
cd "/Users/yuri/Downloads/AI멘토 Jay/開発したAI/WallAI"
git push
cd "/Users/yuri/Downloads/AI멘토 Jay/開発したAI/Studio AI"
git push
```

`[rejected] (fetch first)` が出たら `git --no-pager pull --rebase --autostash && git push`。
**巻き戻りの確認はもう手でやらなくて大丈夫です。** push すると GitHub 側で
「ブログ生成スクリプトの検査」が自動で走り、壊れていれば赤くなってメールが届きます。

**8/22分の記事は、この手順で3アプリとも生成済みです（2026-08-22 09:0x に実行・成功）。**
同じことが必要になったら次のようにします。

1. `https://github.com/piaomingji/reroom-ai/actions` を開く
2. 左の「AI Blog Auto Generator」→ 右上の「Run workflow」→ 緑のボタン
3. 同じことを `piaomingji/WallAI` と `piaomingji/studio-ai` でも行う

3本とも緑になれば、記事と画像が自動でコミットされます。ローカルには `git pull` で降りてきます。

今回の結果: ReRoomAI 29本 / WallAI 27本 / Studio AI 24本、いずれも 2026-08-22 付けの記事と
アイキャッチ画像（50〜230KB）が入っていることを確認済みです。

---

## 残っている作業

### 0. 検査用コミットのプッシュ（3アプリ）

8/22分の記事は入りました。`git pull --rebase --autostash && git push` を3アプリで
実行するだけです。コマンドは冒頭の「次にやること」に書いてあります。

### 1. Talkie（genbatalk）の公開前準備

**この一覧は古い状態です。** Stripe本番化・特商法・実機テストは 2026-08-21 までに、
翻訳精度と有料枠は 2026-08-26 に済んでいます。最新は `Talkie公開前チェックリスト.md` と、
このメモの「Talkie の翻訳精度を直した」を見てください。

- `src/lib/usage.ts` の `FREE_SECONDS_PER_MONTH` を 600 に、`FREE_SESSIONS_PER_DAY` を 20 に戻す（テスト用に上げたまま）
- Stripeを本番キーに切り替え、本番の商品・価格・ウェブフックを設定
- 特定商取引法のページに正式な事業者名を記載
- 新しいアイコンが実際に反映されているか確認（`talkie.smart-ai-portal.com/icon` を直接開く。ホーム画面のアイコンは追加時に焼き付くので再追加が必要）
- 実機（スマホ）でのテスト

**月額プランを売るなら、「お支払い・解約」の導線も必要です。** 他の3アプリに入れたものを
ほぼそのまま流用できます（`app/api/billing-portal/route.ts` と `components/UserNav.tsx`、
`lib/auth.ts` の `linkStripeCustomer` / `getUserByStripeCustomerId`、ウェブフックの
`charge.refunded` と `customer.subscription.updated`）。Talkieは `src/` 配下の構成なので、
パスの読み替えが必要です。

特商法ページに「マイページから解約できます」と書くなら、**書く前に導線を作ってください。**
3アプリでは、書いてあるのに存在しない状態が残っていました。

あわせて、Stripe側も同じ設定が要ります。

- 設定 → Billing → カスタマーポータルを有効化し、「サブスクリプションをキャンセル」をオン、
  タイミングは「請求期間の終了時に」
- ウェブフックのリッスン対象に `checkout.session.completed` / `customer.subscription.deleted` /
  `customer.subscription.updated` / `charge.refunded` の4つ

## 作業上の注意（実際に何度もつまずいた点）

- **私（Claude）はGitのプッシュができません。** 認証情報が無いため、コミットまでは可能ですがプッシュはターミナルからお願いする必要があります。
- **私が触ると `.git/index.lock` が残ります。** git が「Another git process seems to be running」と言ったら `rm -f .git/index.lock .git/HEAD.lock` を実行してください。
- **コマンドは1行ずつ実行してください。** まとめて貼ると行がくっついて `dquote>` で固まります。固まったら `control + C`。
- **Claudeの環境からは `.git/index.lock` を削除できません。** 残っていると `git add` が黙って失敗します。
  Claudeには `開発したAI/_to_delete/` へ退避してもらうか、自分で `rm -f .git/index.lock` を実行してください。
- **`git diff` はページャー（less）に入ります。** そのままコマンドを貼っても実行されず、検索文字列として
  吸われるだけです（「Pattern not found」が出ます）。`q` を2回押して抜けるか、`git --no-pager diff --stat` を使うこと。
  迷ったら `command + N` でターミナルの新しいウィンドウを開くのがいちばん確実です。
- **プッシュが `[rejected] (fetch first)` で弾かれたら**、ブログ自動生成のワークフローが先にコミットしています。
  `git --no-pager pull --rebase --autostash && git push` で通ります。
- **`git pull --rebase --autostash` は要注意。** 一度、修正した内容が古い状態に巻き戻ったことがあります。プッシュ後は内容が反映されているか確認してください。
  2026-08-22、これでブログ生成が3アプリとも1日ぶん止まりました。
  いまは push のたびに `blog-script-check` が走るので、ブログのスクリプトについては自動で見つかります。
- **Vercelの環境変数は追加しただけでは反映されません。** 必ず再デプロイが必要です。
- **フォルダ名にスペースがある `Studio AI` は引用符が必要です:** `cd "/Users/yuri/Downloads/AI멘토 Jay/開発したAI/Studio AI"`
- **Stripeダッシュボードの操作でつまずいた点（2026-08-20）:**
  - 返金は請求書ページの「その他のアクション」には**ありません**。請求書の「最近のアクティビティ」の
    「¥X の支払いが適用されました」をクリック → 支払い詳細 →「返金」。
  - 同じメニューの「未払いとしてマーク」は押さないこと。帳簿を書き換えるだけでお金は戻りません。
  - 取引一覧に直後の支払いが出ないことがあります。請求書から辿るほうが確実です。
  - サブスク一覧の初期表示は「アクティブ」。解約済みは「キャンセル済み」タブにあります。
  - `/api/stripe/webhook` をブラウザで開くと 405 が出ますが、それが正常です（POST専用）。
- **ReRoomAIのウェブフック購読イベント:** `checkout.session.completed` /
  `customer.subscription.deleted` / `customer.subscription.updated` の3件。
- **秘密の値はスクリーンショットに写さないこと。** 一度 `JWT_SECRET` が写り込み、作り直しました。

---

## 困ったときの調べ方

不具合の原因が分からないときは、状態を直接確認できるページを一時的に作ると早いです。
今日は `app/api/debug/store/route.ts` を作り、Redisに繋がっているか・サーバーが今この人を誰と認識しているかを表示させて、
「独自ドメインが古いコードを指している」という真因にたどり着きました（確認後に削除済み）。

同じものが必要になったら「診断ページを作って」と伝えてください。

---

## フリマ出品PRO AI（フリマプロ）— 2026-08-26 公開完了

**5つ目のアプリです。本番の決済まで実地で通し、公開できる状態になっています。**

| 項目 | 内容 |
|---|---|
| フォルダ | `フリマ出品PRO AI（フリマプロ）` |
| 公開アドレス | https://frima.smart-ai-portal.com |
| GitHub | `piaomingji/frima-pro-ai` |
| Vercelプロジェクト | `frima-pro-ai`（チーム: reroom-ai） |
| Stripeアカウント | フリマプロ専用（**他の4つとは別**） |
| 問い合わせ先 | frima.support@gmail.com |
| 構成 | **他の4アプリとは別方式**。Next.jsではなく Vite + React + Vercel Functions |

### 作ったもの

- **Googleログイン**（証明書を署名検証。なりすまし不可）
- **メールアドレス＋パスワード登録**（scryptで暗号化。10回失敗で15分ロック）
- **無料枠 1日3回**。日本時間の午前0時にリセット
- **Proプラン 月1,480円**（Stripeサブスク）。購入・解約・返金まで全自動
- **特商法・利用規約・プライバシーポリシー**（`public/` に静的HTML。フッターからリンク済み）

### 本番で確認したこと（2026-08-26）

実際に1,480円を支払って、4種類のイベントすべてが `200 OK` になることを確認しました。

| 操作 | イベント | 結果 |
|---|---|---|
| 購入 | `checkout.session.completed` | 200 OK → PRO表示 |
| 解約 | `customer.subscription.updated`（ソース: カスタマーポータル） | 200 OK → 期間末まではPROのまま（正しい挙動） |
| 返金 | `charge.refunded` | 200 OK → その場でFREEに戻る |
| 契約終了 | `customer.subscription.deleted` | 実装済み |

### 回数の上限（PRO_HQ_MONTHLY_LIMIT などで変更可）

| プラン | 生成回数 | 高精度モデル |
|---|---|---|
| 無料 | 1日3回 | なし（通常モデル） |
| Pro | 無制限（内部上限 1日100回） | 月200回まで。以降は通常モデルで無制限 |

**「無制限」と書きながら内部に上限を持つので、利用規約の第3条に明記してあります。**
1日20回の高精度にすると月2,400円かかり、手取り1,427円を超えて赤字になるため、
月200回に変えた経緯があります（最悪でも月800〜1,200円に収まります）。

### 無料枠のずる対策

**Googleアカウントを切り替えると無料枠が復活する穴**がありました。
いまは「そのアカウントの回数」と「その回線（IP）の回数」の**多いほう**で数えています。
IPの記録は当日を含む2日で自動削除（プライバシーポリシーに記載済み）。

### 料金表と実装の食い違いを修正

料金表に書いてあるのに**実装されていない機能が2つ**ありました。景品表示法（優良誤認）にあたるため修正済みです。

- 「ジャンル別プロンプトカスタム」— ブラウザに保存するだけで、生成に一切使われていなかった
- 「優先生成・爆速レスポンス」— 実装が存在しなかった

いまはProの中身をこう定義し、**すべて実装済み**です。

- 3つのフリマ（メルカリ／ラクマ／Yahoo!フリマ）用のタイトルを同時生成
- 高値売りに特化した追加指示
- ジャンル別に文章のトーンを指定
- 高精度モデルで生成（月200回まで）
- 写真6枚まで（無料は3枚）

### 直した不具合（2026-08-26）

- **本番でAPIが全部500になっていた** — `/api` の相対importに `.js` が無く、
  Vercelが読み込めていなかった。12ファイルすべてに `.js` を付けて解決
- **デプロイがBlockedになった** — コミットのメールアドレスがGitHubアカウントと
  結び付いていなかった。`git config user.email piaomingji@users.noreply.github.com` で解決
- **グラデーションが黒くつぶれていた** — Tailwind 4 にブランド色を登録しておらず、
  `to-brand-500` が透明になっていた。`src/index.css` の `@theme` に登録して解決。
  ボタン・ロゴ・生成ボタンの3か所が直りました
- **スマホで表示が崩れていた** — ヘッダーが横一列に収まらず「フリマ出品」が縦一列に、
  「ログイン」が縦書きになっていた。狭い画面では上下2段に組み替えて解決

### 便利ファイル（ダブルクリックで実行）

| ファイル | 何をするか |
|---|---|
| `Vercelに環境変数を入れる.command` | `.env` を読んでVercelへ一括投入 |
| `Stripeの鍵を設定する.command` | 鍵を画面に出さずに受け取り、`.env`とVercelへ入れて再デプロイ |
| `更新をGitHubに送る.command` | ロック削除 → add → commit → push |
| `本番にデプロイする.command` | `npx vercel --prod` |

**`✓ Ready` のあとの `Enterキーで閉じます...` は完了の合図です。** 止まっているわけではありません。

### デプロイできたかの確認方法

`public/version.txt` に日付入りの文字列を置いてあります。

```
https://frima.smart-ai-portal.com/version.txt
```

**「画面が変わらない」の原因はほぼ全部ブラウザのキャッシュです。**
本番が更新されているかは必ずこれで確かめてください。
`Command + Shift + R`、それでもダメなら別のブラウザで開くのが確実です。

### これから気にすること

- **Gemini APIの請求を月1回確認する。** 実績を見て `PRO_HQ_MONTHLY_LIMIT`（現在200）を調整
- **Stripeの「支払い」画面が売上そのもの**
- 2026-08-26時点で、運営者本人がProを契約中（実費1,480円）。不要なら全額返金でFREEに戻ります

---

## Talkie の翻訳精度を直した（2026-08-26 / プッシュ済み）

コミットは `a0c4462` → `5d4d57b` → `c8f66e4` の3本です。

### 直したもの

**1. 否定が反転していた。** 「行ったことありません」が3回とも `不，我去过`（行ったことがある）に
なっていました。原因は確定のしかたです。確定してよいかを**訳文の文末記号**で判断していましたが、
中国語は否定が動詞の直前（没）に入るので、生成途中の `不，我去过。` が**完成した文に見えます**。
日本語は否定が文末なので、原文が揃うころには訳が確定済みでした。
`generationComplete` というモデル自身の完了シグナルを見るように変えました。
**このシグナルは、それまで一度も読んでいませんでした。**

**2. 前の発話の訳が次の発話の先頭に混ざっていた。** 訳は音声より遅れて届くので、確定した直後に
届いた残りが次のバッファに積まれていました。`你喜欢吃甜糕吗?` の訳が「何が好きですか?お餅は
好きですか?」で始まっていたのがこれです。確定後1.5秒以内に届いた断片は、前の発話のものとして
捨てるようにしました。

**3. 同音異義語を取り違えても気づけなかった。** 「いった」は 行った とも 言った とも聞こえます。
「行ったことありません」が「言ったことありません」と聞き取られ、`我没说过` と訳されていました。
**これは翻訳の誤りではありません。聞き取りの誤りに、翻訳が忠実に従っただけです。**
音に違いがないので、聞き取りを賢くしても直りません。**直せるのは文脈だけです。**

ここで構造上の問題が分かりました。**通訳のセッションは方向ごとに別々で、互いの発話を聞いていません。**
日本語→中国語のセッションは、直前の中国語の質問を一度も聞いていない。目的語を落とす日本語を、
文脈なしで訳そうとしていたわけです。

そこで直前2ターンを `/api/polish` に渡すようにしました。**両方向の会話が一箇所に集まるのは
ここだけです。** そのうえで、否定の食い違いと同音異義語の取り違えを見つけたら、
**勝手に直さず画面に出します。** 琥珀色の帯が出て、タップすると訳が入れ替わり、読み上げ直します。

どちらか一方を常に正しいと決めると、もう一方の事故が必ず戻ってきます。以前「元気を感じない」が
「元気を感じてる」と聞き取られ、文字起こしを信じた結果、逆の意味で訳された事故がありました。
**だから選ばずに、両方見せて人に決めてもらう作りにしています。**

**4. 有料会員が1日300回で止まっていた。** `/api/translate` と `/api/tts` だけが `assistLimit(isPaid)`
を使わず `300` を直書きしていて、Proの人も無料の人と同じ回数でした。時間が残っているのに読み上げ
だけ止まる状態です。4本すべて揃えました（無料400／有料3000）。`contact` の20回は荒らし避けなので
そのままです。

### 踏んだ罠（同じ判断をすると必ず踏みます）

**モデルの音声を「まだ話している証拠」として扱ってはいけません。**
`liveTranslate.ts` に「音声も活動として数える」というコメントがあり、実装がそうなっていなかったので
直しました。**これが大事故でした。** Gemini はマイクを開けている間ずっと音声を送り続けるので、
そのたびに確定タイマーがリセットされ、**発話がいつまでも確定しなくなりました。** 画面に何も出ず、
45秒後にようやく読み上げられる状態です。元の実装が正しく、コメントのほうが間違っていました。
そもそもこのアプリはモデルの音声を再生していません（`/api/tts` で読み上げています）。
**待つ意味がありません。** コードにこの理由を書き残してあります。

**`gemini-2.5-flash` は既定で思考します。** その分が `maxOutputTokens` から引かれ、JSONモードで
出力が途中で切れると**パースに失敗して黙って下書きを返します。** 判定が一度も走っていないのに
「動いてはいるが何も見つからない」ように見えました。`thinkingConfig: { thinkingBudget: 0 }` を入れ、
上限を4096に上げてあります。

**失敗を記録しないと原因が絞れません。** 最初は当たったときしかログを出していなかったので、
「判定が走って何も見つけなかった」のか「そもそも走っていない」のかが区別できませんでした。
いまは毎回どちらかが出ます。

**そして最大の時間の無駄がこれです。** 修正を commit せずに公開サイトでテストしていました。
何度直しても症状が変わらず、原因を探し続けました。**症状が変わらないときは、まず
`git --no-pager log --oneline origin/main -1` で、試しているものが本当に新しいコードか
確かめてください。**

### `?debug=1` で見る行

アドレスの末尾に `/?debug=1` を付けて開くと、画面下にログ欄が出ます（付けなければ出ません）。
右上の「コピー」で全部取れます。

| 出る行 | 意味 |
|---|---|
| `polish: asking, context NN chars` | 判定を呼んだ。`NN` が0なら文脈が渡っていない（会話の1つ目なら正常） |
| `reading looks settled` | 判定が走って、食い違いなしと判断した |
| `reading unclear: "A" vs "B"` | 食い違いを見つけた。琥珀色の帯が出る |
| `polish: refused, HTTP nnn` | APIが断った。番号で理由が分かる |
| `polish: failed, ...` | 通信そのものが失敗した |
| （どれも出ない） | **新しいコードが動いていない** |

`heard:` と `translated:` はブラウザのコンソールにだけ出ます（ログ欄には出ません）。

### 読み上げの声が中国語だけ壊れていた（同日・追記）

長い文で試したところ、**中国語の読み上げだけが別の変な声になりました。** 日本語は正常です。
原因は最終的にひとつの言葉のずれでした。

**Google は中国語を `cmn-CN` としか呼びません。** 言語ではなく国で名付ける他の言語と違い、
Mandarin という言語名で棚に入っています。このアプリは（ブラウザも翻訳モデルも）`zh-CN` と
呼ぶので、声を指定せずに `zh-CN` で合成を頼むと**その場で断られます**。日本語が無事だったのは、
`ja-JP` が Google と共通の呼び名だったからです。

**日本語で確認しても、中国語が壊れていることがあります。** 言語ごとに通る道が違います。
新しい言語を足したときや読み上げまわりを触ったときは、その言語で🔊を押して
`speech source:` が声の名前になっているか、1回ずつ確かめてください。

直したのは4つです。

1. 声が決まらないときに使う言語名を `cmn-CN` 側に直した（`CATALOGUE_ALIASES`）
2. 声選びに1.2秒の期限を切った。**声選びが、声が出ない理由になってはいけません。**
   探すのをやめるのではなく待つのをやめるので、裏で終わったぶんは次から効きます
3. 声なしで断られたら、声を待ってもう一度頼む。期限は読み上げを遅らせないためのもので、
   すでに失敗したものを救うのを邪魔する理由はありません
4. 断られた理由をログに乗せた。**理由は成功したときの応答にしか乗っておらず、いちばん
   知りたい「全部駄目だったとき」に捨てていました**

**ブラウザ内蔵の声にも別のバグがありました。** 声を探すとき `zh-Hans` で照合していたのに、
ブラウザの声は `zh-CN` と名乗ります。`speechLocale()` で正しい値を計算しておきながら、
使っていませんでした。**中国語の声が入っている端末でも、必ず見つけられません。**
地域まで一致する声を優先し、言語が同じなら受け入れるように直しました。

そして、**その言語の声が無いときは黙るようにしました。** 既定の声は端末の言語で喋るので、
日本語の声が中国語を読み上げていました。訛りではなく意味を成しません。訳は画面に出ているので、
音声だけ諦めるほうがましです。

**言語の呼び名のずれ（次に言語を足すときに読む）**

Google Cloud Text-to-Speech は、いくつかの言語をこのアプリと違う名前で呼んでいます。
`src/app/api/tts/route.ts` の `CATALOGUE_ALIASES` がその対応表です。

| このアプリ | Google | なぜ |
|---|---|---|
| `zh-CN` | `cmn-CN` | 国ではなく言語（Mandarin）で名付けている |
| `zh-TW` | `cmn-TW` | 同上 |
| `zh-HK` | `yue-HK` | 広東語（Cantonese） |
| `ar-SA` | `ar-XA` | 書き言葉が国をまたぐので、地域を置かない |

**アラビア語だけは救済経路からも漏れます。** 中国語は `zh-CN` で問い合わせても Google が
`cmn-CN` の声を返してくれたので、遅い経路でなら動いていました。`ar-SA` は Google にとって
存在しない名前なので問い合わせても空が返り、「声を待って頼み直す」経路も空振りします。
**この表に1行足すことが、アラビア語の唯一の動く道です。**

そしてもうひとつ。`src/lib/languages.ts` の**60以上の言語は2文字コードのまま**です
（`tr`、`uk`、`bn` など）。Google は本来 `tr-TR` のような完全な形を要求します。
これらが動いているのは、**「声なしで断られたら声を待って頼み直す」経路のおかげ**です。
その経路を外すと、まとめて壊れます。

**確認は自分で話さなくてもできます。** B側をその言語にして日本語で一言話し、出てきた訳の
🔊を押して `speech source:` を見るだけです。読み上げは訳された側の言語しか見ていません。

2026-08-26 時点で確認済み: 日本語 `ja-JP` / 中国語 `cmn-CN` / 韓国語 `ko-KR` /
ベトナム語 `vi-VN` / アラビア語 `ar-XA`（すべて Chirp3-HD）。**5言語とも正常です。**

### 琥珀色の帯の誤検知を止めた（同日・追記）

長い文で、否定でも同音異義語でもない**ただの言い換え**に帯が出ました（`最近的`↔`近年来` など）。
正しい訳に「別の意味かも」と出るのは、当たったとき以上に害があります。

そこで、代案が**訂正なのか訳し直しなのか**を機械的に判定します（`isLocalisedChange`）。
共通の前後を除いた差分が8文字以内なら訂正、超えれば訳し直しとして捨てます。
否定は1文字（`我去过`→`我没去过`）、同音異義語も1文字（`说过`→`去过`）です。
**モデルの判断だけに任せず、最後に決定的な物差しを当てています。**

### 読み上げの音量と、失敗が見えなかった件（同日夜・追記）

**Android で読み上げの音量が一定しませんでした。** 最初は小さく、何往復かで大きくなり、また小さくなる。
掘っていったら、本物の欠陥が2つ出てきました。

**1. 読み上げのたびに `AudioContext` を作り直していた。** Android は再生のたびにオーディオフォーカスを
取り直すので、そのつど音量の扱いが変わります。「4回目で大きくなる」「最後に大きくなる」と**回数が
一定しなかった**のはこれです。しかも `onended` で閉じる作りだったので、**再生が始まらなかった場合は
閉じられず**に溜まります。捕音用の `AudioContext` は最初から1つ生かしてあるので、読み上げもそれを
使い回すようにしました。会話中ずっと音声セッションは1つです。

**2. そのうえで、そもそも音が小さかった。** 一定になったあとも YouTube より明らかに小さい。
数字で上げられるのは Cloud TTS の `volumeGainDb` ですが、**10dB あたりから歪むので9で頭打ち**です。
そこでブラウザ側にコンプレッサーを入れました（音源 → コンプレッサー → 2.2倍 → スピーカー）。
ピークを抑えてから持ち上げるので、波形は天井に当たりません。設定は声用です（閾値 -28、ratio 8、
attack 0.003、release 0.2）。**まだ小さければ 2.2 → 3.0 まで上げられます。**

エコーキャンセルも切ってあります（`echoCancellation: false`）。読み上げ中はマイクの音を捨てているので
**このアプリには元々不要**で、Android では出力を通話系統に寄せる副作用だけがありました。
ただし**これだけでは音量問題は直りませんでした**。効いたのは上の1です。

**3. そして最大の発見 — 検知していた失敗が、ひとつも画面に出ていませんでした。**

| アプリが検知していたもの | これまでの表示 |
|---|---|
| マイクを拒否された | 何も出ない |
| 通話に割り込まれてマイクを失った | 何も出ない |
| 通訳セッションが開けなかった | 何も出ない |
| 訳し直しに失敗した | 何も出ない |

`micPermissionError` と `networkError` は**書き込む処理だけがあって、描画する部分が存在しませんでした**。
`t.micDenied` という案内文も用意されていたのに一度も使われていません。ボタンを押しても黙って何も
起きないので、**使う人にはアプリが壊れたのか自分の操作が悪いのか区別できません。**
マイクの拒否は閉じるまで消えない赤い帯に、それ以外は数秒で消える帯にしました。

> **他の3アプリも同じ形になっていないか、一度見てください。** 状態を持たせたのに描画を書き忘れる、
> というのは1箇所でやると他でもやっています。eslint が `assigned a value but never used` として
> ずっと警告していましたが、既存の警告に埋もれて気づけませんでした。

**案内文も直しました。** 「アドレス欄の鍵マークから許可してください」と書いてありましたが、
**ホーム画面から起動するとアドレス欄がありません。** 存在しない場所を指していました。
いまは両方の場合を書いてあります。

### 実機テスト — 2026-08-26 iPhone・Android とも全項目合格

| 項目 | Android | iPhone |
|---|---|---|
| ホーム画面のアイコンから起動して話す | ✅ | ✅ |
| 翻訳中に画面を消し、戻ってボタンで続けられるか | ✅ | ✅ |
| 翻訳中に別アプリへ切り替え、戻れるか（着信も同じ経路） | ✅ | ✅ |
| マイクを拒否したとき、固まらずに案内が出るか | ✅ | ✅ |
| 2人で往復して、前の発話が混ざらないか | ✅ | ✅ |
| 長い説明が細切れに割れないか | ✅ | ✅ |

割り込みからの復帰は iPhone でこそ見る価値があります。**8/21 に見つかった `AudioContext` の
resume 漏れは iOS の不具合**で、Safari は中断の扱いが Android と違うためです。

**ホーム画面から起動したアプリは「再読み込み」ができません。** 最近のアプリ一覧から完全に終了
させないと古いページが残ります。「直したのに変わらない」の半分はこれでした。

### 2026-08-26 のコミット（18本・すべて push 済み）

```
a0c4462 否定の反転／前の発話の混入／同音異義語の検知
5d4d57b 音声で確定が止まる不具合（私が入れた退行）
c8f66e4 有料会員が1日300回で止まる穴
440d77f 帯の誤検知の遮断、間違った声で読ませない
ed9cf62 声選びが読み上げを止めないようにする
094a472 中国語を Google の呼び名（cmn-CN）で頼む
0f0e9b2 アラビア語も同様（ar-XA）
85c3bab 右から左の言語のレイアウト、言語名の併記
33b44ad ?debug=1 を記憶し、インストール版でも出す
a62523e エコーキャンセルを切る
0a9b293 音声セッションを会話中1つにする
980cdde 読み上げを +6dB
8ce2ce4 +9dB（YouTube と比べて）
53368d9 コンプレッサー + 2.2倍
3e3acf1 失敗を画面に出す
4694ee5 用語集の復活（音の近さでも効かせる）
1b2d7b1 誰も言わない語は、それ自体が聞き間違いの証拠
1498b11 判定に思考の予算を戻す（未検証）
```

### まだ確かめていないこと

**5分の通し確認は 2026-08-26 に1回やりました。** 文が細切れに割れないこと、訳の末尾を
捨てていないこと、両方向とも Chirp3-HD で読まれることは確認済みです。残りはこれです。

- 読み上げは5言語で確認済みです。**他の言語を足すときは、その言語で1回🔊を押すこと**
- **帯の誤検知が本当に止まったか。** 8文字という物差しは実例5件で検証しただけです
- 長い会話が続いたときの帯の出かた（文脈は直前2ターンしか渡していません）
- **読み上げの音量。** コンプレッサーを入れたあとの聞き比べが未確認です。まだ小さければ
  増幅を 2.2 → 3.0 に上げられます
- **他の3アプリで、エラーが画面に出ているか。** Talkie では全部出ていませんでした

### 直していない、分かっている不揃い

帯をタップすると訳は入れ替わりますが、**その下の日本語（文字起こし）は元のままです。**
`/api/polish` に文字起こしの訂正版も返させれば揃います。

### 今回入れたものの限界

**「間違えなくなる」仕組みではありません。気づいて見せる仕組みです。**
日本語→中国語のセッションは相変わらず相手側の発話を聞いていないので、目的語が落ちた文や
同音異義語は**これからも間違えます**。安全網を1枚張っただけです。
根本から直すなら、ライブセッションに相手側の文脈を渡す方法を探すことになります。

---

## 未解決の課題 — 音の近い語の取り違え（2026-08-26 時点）

### 何が起きているか

```
話した言葉:    收据（shōujù／レシート）
文字起こし:    手机（shǒujī／携帯）  または  手月（中国語に存在しない）
訳:            携帯 / 手帳
```

**翻訳は壊れていません。聞き取りが壊れていて、翻訳がそれに忠実に従っています。**
音がほとんど同じなので、大きな声で言い直しても同じ答えが返ります。「行った／言った」と
同じ構図ですが、**完全な同音ではなく「音が近い」**ぶん厄介です。

### 打った手（順に）

1. **直前2ターンを `/api/polish` に渡す。** 通訳セッションは方向ごとに別々で互いを聞いていないので、
   両方向の会話が集まるのはここだけ
2. **同音異義語も食い違いの検知対象に加える**（琥珀色の帯で見せる）
3. **用語集を復活させた**（`4694ee5`）。設定画面で登録でき、校正パスに
   「登録語と音が近く、文脈が合うならそちらを優先」と指示してある
4. **「誰も言わない語」自体を証拠にする**（`1b2d7b1`）。`手月` は中国語ではないので、
   それだけで聞き間違いと判断してよい、と書いた
5. **思考の予算を戻した**（`1498b11`）。← **未検証。ここから始めてください**

1〜4を入れてもログは `reading looks settled` のままでした。原因は5だと考えています。
**別件（JSONが途中で切れる）の対処で `thinkingBudget: 0` を入れたのですが、JOB 2 は
まさに推論の仕事です。** 考える余地を与えずに「これは誰も言わない語か」と尋ねれば、
返ってくるのは「問題なし」です。プロンプトをいくら強めても届きません。

### 5でも駄目だったときの次の一手

**モデルの判断に頼るのをやめます。** 校正パスに「食い違いがあるか」を尋ねる代わりに、
**文字起こしを全文そのまま翻訳し直し、ライブの訳と並べて、違えば必ず両方見せる**。
帯が出る頻度は上がりますが、判断を挟まないぶん確実です。

これをやるなら、`refineTranslation` の隣に `/api/translate` を呼ぶ経路を足し、
`isLocalisedChange` で「訂正か訳し直しか」を機械的に選り分けるところは今のまま使えます。

### 用語集について知っておくこと

- **端末ごとに保存されます**（`localStorage`）。Android で登録しても iPhone には入りません。
  ログインしている人だけでも端末をまたげるようにするのは**未実装**です
- Live Translate モデルには指示を入れられないので、用語集が効くのは**校正パスだけ**です。
  聞き取りそのものは変えられません
- `extract-terms` は 410 を返す殻のままです。用語の自動抽出は復活させていません

### 正直に残しておくこと

**この件で5回外しています。** こちらの環境から Gemini API に到達できないため、判定の実挙動を
確かめられず、推測とプッシュを繰り返しました。**同じやり方を続けないでください。**
プロンプトを1行変えて試す、を繰り返すより、上の「次の一手」のように**モデルの判断を挟まない
設計に倒す**ほうが早く終わります。

---

## 次に新しいアプリを作るとき

**`開発したAI/新しいアプリを作るときの型.md` を読んでください。**

フリマプロで確立した手順を、順番どおりに全部書いてあります。
Claudeには**この1行を伝えるだけ**で共有できます。

> 開発したAIフォルダの「新しいアプリを作るときの型.md」を読んで、この型どおりに作って。

構成・ファイル分担・環境変数・Google/Stripe/Redisの設定手順・法的ページ・
公開前チェックリスト・料金と原価の考え方・**今回踏んだ地雷の一覧**まで入っています。
