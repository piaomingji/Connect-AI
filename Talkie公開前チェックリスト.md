# Talkie 公開前チェックリスト

作成: 2026-08-20 ／ 対象: `開発したAI/genbatalk`（talkie.smart-ai-portal.com）

コードの修正はこちらで済ませてあります。**下の作業はあなたのターミナルとブラウザでしか
できないもの**だけを並べてあります。上から順に進めれば公開まで届きます。

---

## 0. 先に: 修正をプッシュする — ✅ この回のぶんは完了（`330c2fa`）

> **2026-08-26 追記。** 下の表の7ファイルはプッシュ済みです。この節は手順の見本として
> 残してあります。**そのあとの修正も 5-3 のぶんまで `c8f66e4` で反映済みです。**
> 新しく直したときは、この手順をなぞってください。

私はプッシュができません。ターミナルで1行ずつ実行してください。

```
cd "/Users/yuri/Downloads/AI멘토 Jay/開発したAI/genbatalk"
git --no-pager status --short
git add -A
git commit -m "Handle refunds, keep the billing portal reachable, cap the assist endpoints"
git push
```

- `Another git process seems to be running` と言われたら `rm -f .git/index.lock .git/HEAD.lock`
- `[rejected] (fetch first)` なら `git --no-pager pull --rebase --autostash && git push`
- プッシュ後、Vercel のデプロイが緑になるまで待ってから次へ進んでください

**今回の修正内容（7ファイル）**

| ファイル | 変更 |
|---|---|
| `src/lib/stripe.ts` | GET/DELETE に対応。顧客のメール検索・購読一覧・即時解約を追加 |
| `src/lib/usage.ts` | 顧客ID→アカウントの逆引き、失効した契約も読める関数、加入時の残高リセット、補助APIの1日上限 |
| `src/app/api/stripe/webhook/route.ts` | `charge.refunded` を追加。解約時も顧客IDを残す。プラン変更時に残り時間を満額へ戻す |
| `src/app/api/billing-portal/route.ts` | 失効中でもポータルを開ける。顧客IDが無ければメールから引き当て |
| `src/app/api/polish/route.ts` | 1日の上限と入力文字数の上限を追加 |
| `src/app/api/furigana/route.ts` | 1日の上限を追加 |
| `src/app/tokushoho/page.tsx` | 通訳時間の更新タイミングの記載を実装に合わせて修正 |

いずれも本番ビルド（`next build`）が通ることを確認済みです。

---

## 1. Stripe を本番に切り替える

**アカウントを間違えないこと。** 左上のアカウント名が **Talkie** になっているか、
作業のたびに確認してください。他の3アプリとは別アカウントです。

### 1-1. 商品と価格を本番モードで作る

右上のトグルを「テスト環境」から本番に切り替えてから作成します。
テストモードで作った価格IDは本番では使えません。

| プラン | 周期 | 価格 |
|---|---|---|
| Plus | 月額 | 7.99 USD |
| Plus | 年額 | 76.68 USD |
| Pro | 月額 | 19.99 USD |
| Pro | 年額 | 191.88 USD |

作成後、4つの価格ID（`price_` で始まる文字列）を控えてください。

### 1-2. ウェブフックを登録する

- 宛先URL: `https://talkie.smart-ai-portal.com/api/stripe/webhook`
- リッスンするイベント（**4つ**）
  - `checkout.session.completed`
  - `customer.subscription.updated`
  - `customer.subscription.deleted`
  - `charge.refunded`
- 登録後に表示される署名シークレット（`whsec_` で始まる）を控えてください

`customer.subscription.created` は購読していなくても構いません（`updated` と同じ処理です）。

### 1-3. カスタマーポータルを有効にする

設定 → Billing → カスタマーポータル

- 「サブスクリプションをキャンセル」をオン
- キャンセルのタイミングは **「請求期間の終了時に」**
- 請求履歴の表示をオン

### 1-4. Stripe Tax の設定（`automatic_tax` を有効にしているため必須）

コードがチェックアウトで `automatic_tax[enabled]=true` を送っているので、
**設定 → 税金** のページを一度完了させておく必要があります。最低限これだけです。

- **本社所在地（head office）** を確認して確定する（Stripeの事業所住所が初期値で入っています）
- **既定の商品税コード** を選ぶ。Talkieは電子的に提供される役務なので
  「SaaS」または「Digital services」系のコードが該当します
- 税の扱い（tax behavior）は既定のままで構いません。USD建てなので「税別」扱いになります

**登録（registration）は、追加しなければ税額0で計算されます。** Stripeのドキュメントに
「顧客の所在地に登録が無ければ計算結果は税額0になる」と明記されています。決済が失敗するわけでは
ありません。課税事業者として消費税を上乗せする段になったら、**税金 → 拠点** で日本を追加します。

**方針は決定済み（2026-08-20）: 有効のまま、拠点の登録は追加しない。**
本社所在地と商品税コードだけ確定させ、税額0で運用します。表示価格がそのまま請求されます。
課税事業者になったら、税金 → 拠点 で日本を追加するだけで切り替わります（コード変更は不要）。

税務上どう扱うべきかは私には判断できません（税理士ではありません）。判断が変わったら
言ってください、コードから `automatic_tax` を外すこともできます。

---

## 2. Vercel の環境変数

Production に次が入っているか確認してください（Settings → Environment Variables）。

**必須**

| 変数 | 中身 |
|---|---|
| `GEMINI_API_KEY` | Google AI Studio のキー |
| `KV_REDIS_URL` | Vercel が発行した `redis://` のURL |
| `AUTH_GOOGLE_ID` / `AUTH_GOOGLE_SECRET` | Google OAuth |
| `AUTH_SECRET` | セッション署名鍵。無いと本番でログインが壊れます |
| `STRIPE_SECRET_KEY` | **`sk_live_` で始まること**（`sk_test_` なら本番になっていません） |
| `STRIPE_WEBHOOK_SECRET` | 1-2 で控えた `whsec_` |
| `STRIPE_PRICE_PLUS_MONTHLY` | 1-1 の価格ID |
| `STRIPE_PRICE_PLUS_YEARLY` | 〃 |
| `STRIPE_PRICE_PRO_MONTHLY` | 〃 |
| `STRIPE_PRICE_PRO_YEARLY` | 〃 |

**あとから追加したもの**

| 変数 | 中身 |
|---|---|
| `GOOGLE_TTS_API_KEY` | Cloud Text-to-Speech のキー（プロジェクト `fresh-office-457104-v7`）。読み上げの声に使います。未設定なら `GEMINI_API_KEY` を試し、それも駄目なら旧来の声に戻ります |
| `OWNER_EMAILS` | 無料枠の計測から外すアカウント（カンマ区切り）。本番の検証用。空なら誰も対象外になりません |

**任意（未設定なら括弧内の既定値）**

`FREE_SECONDS_PER_MONTH`（600）、`SIGNED_IN_SECONDS_PER_MONTH`（1800）、
`FREE_SESSIONS_PER_DAY`（20）、`FREE_IP_MULTIPLIER`（4）、
`ASSIST_CALLS_PER_DAY`（400）、`PAID_ASSIST_CALLS_PER_DAY`（3000）

> **テスト用に上げたままの値がないか、ここで確認してください。**
> 本番の `/api/me` は現在 `allowanceSeconds: 600` を返しているので、
> 無料枠の秒数は既定値のままです。`FREE_SESSIONS_PER_DAY` は外から見えないので、
> 画面で確認するか、変数ごと削除して既定の20に戻すのが確実です。

**環境変数は追加・変更しただけでは反映されません。必ず再デプロイしてください。**

---

## 3. 特商法ページ — ✅ 2026-08-21 完了

特定商取引法11条は事業者の氏名（名称）の記載を求めており、個人事業主の場合、
**屋号だけでは要件を満たしません**（消費者庁の解説による）。会社を作る必要はなく、
本名を書けば足ります。所在地と電話番号は「請求があれば遅滞なく開示」で構いませんが、
氏名にこの省略は使えません。「代表者名」の欄が請求時開示になっていたのが不足でした。

4アプリすべて同じ形に直してあります。

| 欄 | 変更後 |
|---|---|
| 事業者名 | ○○運営事務局**（個人事業主）** |
| 代表者名 | **吉澤 友里** |
| 所在地・電話番号 | 請求があった場合、遅滞なく開示（従来どおり・適法） |

対象ファイル: `genbatalk/src/app/tokushoho/page.tsx`、`ReRoomAI/app/tokushoho/page.tsx`、
`WallAI/app/tokushoho/page.tsx`、`Studio AI/app/tokushoho/page.tsx`

> 将来法人化した場合は、事業者名を法人名に、代表者名を代表取締役名に差し替えてください。
> 法人には所在地・電話番号の請求時開示が使えない場面があるので、そのときは併せて見直しを。

**「役務の引き渡し時期」の文言は今回直しました。** 「ご契約日を起点とする1か月ごとに更新」と
書いてありましたが、実装は暦の月替わりでリセットです。「ご契約と同時に満額付与、以後は毎月1日
（日本時間 午前9時）に更新」に書き換えてあります。

あわせて確認しておきたい点がひとつ。特商法ページには
「定期課金の解約は設定画面より手続きでき」と書いてあります。
**この導線は実在します**（設定 → プラン欄の「プランを管理」ボタン → Stripeのポータル）。
他の3アプリで起きた「書いてあるのに無い」状態にはなっていません。

---

## 4. 決済の実地テスト — ✅ 2026-08-20 完了

**本番キーで、購入 → 使用 → 解約 → 返金 まで実際に通しました。全項目クリアです。**

| 確認したこと | 結果 |
|---|---|
| 翻訳した時間が記録されるか | 50秒使って `usedSeconds: 50` |
| 購入でPlusになるか | `plan: plus` / `allowanceSeconds: 3600` |
| 加入時に残高が満額に戻るか | 50 → **0** |
| ポータルから解約できるか | 「09/20 に終了します」表示 |
| 解約後もPlusを維持するか | **`plan: plus` のまま** |
| 返金でプランが戻るか | `plan: free` / `1800` |
| 返金で契約も止まるか | **キャンセル済みへ移動** |

支払った$7.99は返金済み。ウェブフックの失敗は0件。

**この日つまずいた3点（再テストするときのために）**

1. **`customer.subscription.created` を購読しないと、プランが無言で付きません。**
   最初の本番購入がこれで失われました（決済成功・Stripe失敗0件・アプリは無料のまま）。
   コード側は直したので、いまはどのイベントが届いても付きます。それでも5件とも購読すること。
2. **Stripe管理画面からの解約は「即時」です。** 「請求期間の終了時に」という設定は
   **ポータルにしか適用されません。** 解約の挙動を試すときは、必ずアプリの
   設定 →「プランを管理」から通してください。
3. **テスト環境で買ったプランは、本番に切り替えると幽霊になります。**
   本番のStripeがその契約を知らないため、解約も返金も永久に伝わりません。
   期限まで有料プランのまま残ります。

## 5. 実機テスト — ✅ 2026-08-21 完了

iPhone と Android で通しました。**合格:** マイク拒否時の挙動、ホーム画面からの起動、
音声再生、画面消灯からの復帰、2人での会話。

**見つけて直したもの:** 通話後にマイクが復帰しない（AudioContextのresume漏れ）、
画面にマイクまで収まらない（`100vh`→`100dvh`）、初回7秒の待ち（マイクとセッションの並行化）、
読み上げの声（Cloud Text-to-Speech に差し替え）。詳細は引き継ぎメモに記載。

**端末だけで起きる不具合は `?debug=1` を付けて開くと画面下にログが出ます。**

> **2026-08-26 追記 — やり直して、両端末とも全項目合格しました。**
> 5-3 の変更で、確定の待ち方と読み上げ中のマイクの締め方に手を入れました。
> **割り込みが入ったときの復帰は、そこで壊れうる場所です**（過去に一度壊れています）。
>
> | 項目 | Android | iPhone |
> |---|---|---|
> | ホーム画面のアイコンから起動して話す | ✅ | ✅ |
> | 翻訳中に画面を消し、戻ってボタンで続けられるか | ✅ | ✅ |
> | 翻訳中に別アプリへ切り替え、戻れるか（着信も同じ経路） | ✅ | ✅ |
> | マイクを拒否したとき、固まらずに案内が出るか | ✅ | ✅ |
> | 2人で往復して、前の発話が混ざらないか | ✅ | ✅ |
> | 長い説明が細切れに割れないか | ✅ | ✅ |
>
> 割り込みからの復帰は iPhone でこそ見る価値があります。**2026-08-21 に見つかった
> `AudioContext` の resume 漏れは iOS の不具合**で、Safari は中断の扱いが Android と違います。
> Android で通ったことは iPhone の保証になりません。
>
> **ホーム画面から起動したアプリは再読み込みができません。** 直したはずのものが変わらないときは、
> 最近のアプリ一覧から完全に終了させてから開き直してください。
>
> **マイクの権限は、アプリとして開いた場合は Android のアプリ権限側にあります**
> （アイコン長押し → アプリ情報 → 権限 → マイク）。ブラウザのアドレス欄の鍵マークではありません。

## 5-2. アイコンの確認

- ブラウザで `https://talkie.smart-ai-portal.com/icon` を直接開く
  → 濃紺の背景に吹き出し2つが出れば新しいアイコンです
- マニフェスト（`/manifest.webmanifest`）が `/icon` を指していることは確認済みです
- **ホーム画面のアイコンは追加した時点の絵が焼き付きます。**
  古いままなら一度削除して追加し直してください

---

## 5-3. 翻訳の精度を直した — ⚠️ 2026-08-26 修正済み・検証は1ケースのみ

**3つの不具合を直してプッシュしました**（`a0c4462` / `5d4d57b` / `c8f66e4`）。
理由と踏んだ罠は引き継ぎメモの「Talkie の翻訳精度を直した」に書いてあります。

| 症状 | 原因 | 直し方 |
|---|---|---|
| 「行ったことありません」→ `不，我去过`（否定が消える） | 確定してよいかを訳文の文末記号で判断していた。中国語は否定が動詞の前に入るので、途中の `不，我去过。` が完成した文に見える | モデルの `generationComplete` を見るようにした。**それまで一度も読んでいなかった** |
| 前の発話の訳が次の発話の頭に混ざる | 訳は音声より遅れて届くのに、確定時にバッファを空にするだけだった | 確定後1.5秒以内の断片は前の発話のものとして捨てる |
| 「行った」を「言った」と聞き取り `我没说过` になる | 音では区別できない同音異義語。**翻訳は正しく、聞き取りが誤っていた** | 直前2ターンを `/api/polish` に渡し、食い違いを見つけたら**勝手に直さず琥珀色の帯で見せる**。タップで入れ替わる |

**有料会員が1日300回で止まっていた穴もふさぎました。** `/api/translate` と `/api/tts` だけが
`assistLimit(isPaid)` を使わず `300` を直書きしていました。4本すべて揃えてあります。

### 5分の通し確認でこうなりました（2026-08-26）

| 見た点 | 結果 |
|---|---|
| 長い文が細切れに割れないか | **問題なし。**1文が1メッセージにまとまりました |
| 訳の末尾を捨てていないか | **形跡なし。**`trailing fragment` は0件 |
| 帯の誤検知 | **出ました → 遮断済み**（差分8文字を超える代案は捨てる） |
| 中国語の読み上げ | **壊れていました → 修正済み**（`cmn-CN` の呼び名のずれ） |
| 両方向の声 | `cmn-CN-Chirp3-HD` / `ja-JP-Chirp3-HD` で確認 |

**読み上げも直しました。** 中国語だけ声が出ずに変な音声になっていました。Google は中国語を
`cmn-CN` としか呼ばないのに `zh-CN` で頼んでいたためです。**日本語で確認しても中国語が
壊れていることがあります。** 詳細は引き継ぎメモへ。

**音量も直しました。** 読み上げのたびに `AudioContext` を作り直していたため、Android で音量が
再生ごとにばらつきました。いまは会話中ずっと1つです。そのうえで合成を +9dB にし、ブラウザ側に
コンプレッサーを入れてあります（10dB を超えると歪むので、数字だけでは足りませんでした）。

**そして、アプリが検知していた失敗が、ひとつも画面に出ていませんでした。** マイクの拒否も、
通話による中断も、接続の失敗も、状態を書き込む処理だけがあって描画がありませんでした。
ボタンを押しても黙って何も起きない状態です。**他の3アプリも同じ形になっていないか見てください。**

### 未解決 — 音の近い語の取り違え

中国語で 收据（レシート）と言うと 手机（携帯）や 手月（存在しない語）と聞き取られ、
携帯・手帳と訳されます。**翻訳ではなく聞き取りの問題**で、言い直しても抜けられません。

用語集を復活させ、「誰も言わない語は聞き間違いの証拠」とも指示しましたが、まだ直っていません。
**最後に打った手（校正パスの思考の予算を戻す・`1498b11`）が未検証です。** 引き継ぎメモの
「未解決の課題」に、経緯と、駄目だったときの次の一手を書いてあります。

**公開の可否には関わりません。** 会話は成立しますし、他の言い方をすれば通じます。ただ
「同じ言葉を何度言っても直らない」は使う人の信頼を削るので、公開後も残す価値のある課題です。

### まだ確かめていません

実機で通したのは**1ケース1往復だけ**です。公開前に、`?debug=1` を付けて5分ほど普通の会話を
1回通してください。アドレスの末尾に `/?debug=1` を足すと画面下にログ欄が出ます（右上の
「コピー」で全部取れます）。

- **1つの文が複数のメッセージに割れていないか。** `generationComplete` が発話ごとに来るのか
  区切りごとに来るのか分かっていません。後者なら細切れになります
- 読み上げは 2026-08-26 に5言語で確認済み（`ja-JP`・`cmn-CN`・`ko-KR`・`vi-VN`・`ar-XA`、
  すべて Chirp3-HD）。**言語を足したら、その言語で1回🔊を押してください。**
  その言語を話せなくても確かめられます — B側をその言語にして日本語で一言話し、
  出てきた訳の🔊を押すだけです
- 帯の誤検知が本当に止まったか（物差しは実例5件で検証しただけです）
- **読み上げの音量。** 合成を +9dB にし、ブラウザ側にコンプレッサー（2.2倍）を入れました。
  YouTube と同じ端末・同じ音量で聞き比べてください。まだ小さければ 2.2 → 3.0 に上げられます
- **iPhone / Android の実機テスト（5章）は、今回の変更後にやり直していません**

### 帯をタップしたときの不揃い（既知・未修正）

訳は入れ替わりますが、**下の文字起こしは元のままです。**

---

## 6. 再テストするときの観点

一度通っていますが、大きく手を入れたあとはここを見てください。

- マイクを**拒否**したときに画面が固まらないか、案内が出るか
- **ホーム画面から起動**した状態（standalone）でマイクと音声が動くか
- 翻訳中に**画面を消す・電話に出る・別アプリに切り替える** → 戻ってボタンで復帰するか
- iOS Safari と Android Chrome の両方
- **各言語で🔊を押して、`speech source:` が声の名前になっているか**（`legacy` や失敗なら要調査）。
  2026-08-26、中国語だけがここで壊れていました。**言語ごとに通る道が違います**
- **発話がすぐ確定して読み上げられるか。** 何秒も待たされるなら確定のしかたを壊しています
- **1つの文が複数のメッセージに割れていないか**（5-3 参照）
- `?debug=1` のログに `polish: refused` や `polish: failed` が出ていないか

## 積み残し（公開後でよいもの）

- **一部返金は自動で戻しません。** 全額返金のときだけプランを戻します。
  金額を指定した返金は「値引き」なのか「取り消し」なのか判断できないためです
- **`invoice.payment_succeeded` は未処理です。** 今は毎月の請求で何もする必要が
  ないので実害はありませんが、月ごとに残り時間をリセットする作りに変えるなら必要になります
  （現在は暦月で数えているので、月初に自然に戻ります）
- **Redis が落ちると上限が効かなくなります。** これは意図した設計（翻訳を止めないため）
  ですが、そのぶん請求は青天井です。4-4 の確認は公開後もときどきやってください
- ~~`/api/translate` と `/api/tts` は有料会員も1日300回のまま~~
  → **2026-08-26 に修正済み。** 4本すべて `assistLimit(isPaid)`（無料400／有料3000）に揃えました
- **琥珀色の帯をタップしても、下の文字起こしは直りません。** 訳だけが入れ替わります。
  `/api/polish` に文字起こしの訂正版も返させれば揃います（5-3 参照）
- **通訳のセッションは方向ごとに別々で、互いの発話を聞いていません。** 目的語が落ちた文や
  同音異義語は今後も間違えます。今回入れたのは安全網であって、根本の修正ではありません
